class Initialize {
	Grid init_grid;

	Initialize() {
		init_grid = new Grid(5, 5, 80);
	}

	void run(PGraphics pg) {
		int start = millis();
		int init_length = 10000;
		// init_grid.generate_display(pg);
	}
}